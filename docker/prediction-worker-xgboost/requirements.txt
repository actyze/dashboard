fastapi==0.140.0
uvicorn==0.51.0
# xgboost-cpu, not xgboost: the default distribution declares
# nvidia-nccl-cu12 on Linux, which is proprietary and unusable in a
# CPU-only worker. Same package name on import, same version.
xgboost-cpu==2.1.3
pandas==2.2.3
scikit-learn==1.7.2
numpy==2.3.5
trino==0.329.0
psycopg2-binary==2.9.11

# Observability dependencies (shared)
structlog==24.4.0
python-json-logger==3.2.1
prometheus-client==0.24.1
