{{- if .Values.secrets.create }}
---
# External LLM API Key Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dashboard.fullname" . }}-external-llm
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.secrets.externalLLM.apiKey }}
  external-llm-api-key: {{ .Values.secrets.externalLLM.apiKey | b64enc | quote }}
  {{- else }}
  external-llm-api-key: {{ "placeholder-change-me" | b64enc | quote }}
  {{- end }}

---
# Trino Credentials Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dashboard.fullname" . }}-trino-credentials
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.secrets.trino.username }}
  username: {{ .Values.secrets.trino.username | b64enc | quote }}
  {{- else }}
  username: {{ "trino_user" | b64enc | quote }}
  {{- end }}
  {{- if hasKey .Values.secrets.trino "password" }}
  password: {{ .Values.secrets.trino.password | b64enc | quote }}
  {{- else }}
  password: {{ "CHANGE_ME" | b64enc | quote }}
  {{- end }}

{{- if .Values.services.demo.enabled }}
---
# Demo Database Credentials Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dashboard.fullname" . }}-demo-credentials
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  postgres-username: {{ .Values.secrets.demo.postgres.username | default "demo_user" | b64enc | quote }}
  postgres-password: {{ .Values.secrets.demo.postgres.password | default "demo_password" | b64enc | quote }}
  mysql-username: {{ .Values.secrets.demo.mysql.username | default "demo_user" | b64enc | quote }}
  mysql-password: {{ .Values.secrets.demo.mysql.password | default "demo_password" | b64enc | quote }}
  mysql-root-password: {{ .Values.secrets.demo.mysql.rootPassword | default "root_password" | b64enc | quote }}
{{- end }}

{{- end }}
