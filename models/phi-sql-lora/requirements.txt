# Phi-4-mini LoRA Training for Trino 477 + ONNX
# Aligned with phi-sql model dependencies

# Core ML dependencies (matching phi-sql versions)
torch==2.5.1
transformers>=4.47.0
tokenizers>=0.20.0
accelerate>=0.20.0

# ONNX dependencies (matching phi-sql)
onnxruntime==1.19.2
optimum[onnxruntime]>=1.23.2

# LoRA training specific
peft>=0.7.0
datasets>=2.16.0
bitsandbytes>=0.41.0

# Additional utilities
numpy>=1.24.0
tqdm>=4.66.0
einops>=0.6.0
packaging>=21.0

# FastAPI server dependencies
fastapi>=0.104.0
uvicorn>=0.24.0

# Note: Compatible with phi-sql model for seamless ONNX integration
